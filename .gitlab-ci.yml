image: alpine:latest

stages:
  - deploy

deploy_to_planethoster:
  stage: deploy
  script:
    - apk add --update bash curl
    - curl -o lftp.tar.gz http://lftp.yar.ru/ftp/lftp-4.9.3.tar.gz
    - tar -xzf lftp.tar.gz
    - cd lftp-4.9.3
    - ./configure
    - make && make install
    - lftp -c "set ftp:ssl-allow no; open -u kxbshafa,BYC8qeaGyswSZ9 cargofest.go.yj.fr; mirror -Rnev ./C/Users/bilk7/OneDrive/Bureau/Cours/PWeb/Projet/projet_blablacar /public_html"
